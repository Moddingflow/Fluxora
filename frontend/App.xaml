<Application x:Class="Fluxora.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Assets/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Brand typography. Embedded TTFs (see Fonts/), full Latin + Cyrillic + German
                 so RU/EN/DE render in-brand. Segoe kept as graceful fallback.
                 Onest keeps the original Fluxora feel while avoiding the old ultra-wide
                 display face on dense UI text. -->
            <FontFamily x:Key="FluxoraFontBody">./Fonts/#Onest, Segoe UI Variable, Segoe UI</FontFamily>
            <FontFamily x:Key="FluxoraFontDisplay">./Fonts/#Onest, Segoe UI Variable, Segoe UI</FontFamily>

            <SolidColorBrush x:Key="ChromeBrandBrush" Color="#F4F8FF" />
            <SolidColorBrush x:Key="IconTileBrush" Color="#091C35" />
            <SolidColorBrush x:Key="IconTileBorderBrush" Color="#305B94" />
            <SolidColorBrush x:Key="IconTileForegroundBrush" Color="#F4F8FF" />
            <SolidColorBrush x:Key="ProgressTrackBrush" Color="#0D1420" />
            <SolidColorBrush x:Key="ProgressTrackBorderBrush" Color="#1E3555" />
            <SolidColorBrush x:Key="ProgressPulseBorderBrush" Color="#6FA5FF" />
            <SolidColorBrush x:Key="ToggleOnBrush" Color="#4D8DF7" />
            <SolidColorBrush x:Key="ToggleOnHoverBrush" Color="#6FA5FF" />
            <SolidColorBrush x:Key="ToggleOnBorderBrush" Color="#6FA5FF" />
            <SolidColorBrush x:Key="ToggleThumbOnBrush" Color="#F4F8FF" />

            <!-- Keep text rendered as text: fixed hinting, ClearType, and pixel snapping
                 favor sharp small UI labels over bitmap-like scaling artifacts. -->
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontFamily" Value="{StaticResource FluxoraFontBody}" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
                <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Style.Triggers>
                    <Trigger Property="FontWeight" Value="Black">
                        <Setter Property="FontFamily" Value="{StaticResource FluxoraFontDisplay}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="{x:Type Button}">
                <Setter Property="FontFamily" Value="{StaticResource FluxoraFontBody}" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
                <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
