<Window x:Class="Fluxora.App.Views.StartupSplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fluxora"
        Width="440"
        Height="286"
        ResizeMode="CanMinimize"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource ChromeBrush}"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Fluxora.ico"
        FontFamily="{StaticResource FluxoraFontBody}"
        FontWeight="Medium"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        RenderOptions.ClearTypeHint="Enabled"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        SourceInitialized="OnSourceInitialized">
    <Window.Resources>
        <Style x:Key="StartupProgressBarStyle" TargetType="{x:Type ProgressBar}">
            <Setter Property="Height" Value="7" />
            <Setter Property="Foreground" Value="{DynamicResource SplashAccentBrush}" />
            <Setter Property="Background" Value="{DynamicResource SplashProgressTrackBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="3.5" />
                            <Border x:Name="PART_Track"
                                    Background="Transparent"
                                    CornerRadius="3.5" />
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3.5" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource SplashWindowBrush}"
          ClipToBounds="True">
        <Border BorderBrush="{DynamicResource ChromeLineBrush}"
                BorderThickness="1" />

        <Grid Margin="36,30,36,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="20" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image Source="pack://application:,,,/Fluxora.png"
                   Width="58"
                   Height="58"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   HorizontalAlignment="Center" />

            <TextBlock Grid.Row="2"
                       Text="{Binding AppName}"
                       FontFamily="{StaticResource FluxoraFontDisplay}"
                       FontSize="31"
                       FontWeight="Black"
                       Foreground="{DynamicResource SplashTextBrush}"
                       TextAlignment="Center"
                       HorizontalAlignment="Center" />

            <StackPanel Grid.Row="4">
                <TextBlock Text="{Binding Title}"
                           FontSize="16"
                           FontWeight="Black"
                           Foreground="{DynamicResource SplashTextBrush}"
                           TextAlignment="Center"
                           TextWrapping="Wrap" />
                <TextBlock Text="{Binding Detail}"
                           Margin="0,7,0,0"
                           FontSize="12.5"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SplashSecondaryTextBrush}"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="360" />
            </StackPanel>

            <Grid Grid.Row="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="7" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid>
                    <TextBlock Text="Загрузка"
                               FontSize="11"
                               Foreground="{DynamicResource SplashMutedTextBrush}"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding ProgressText}"
                               FontSize="11"
                               Foreground="{DynamicResource SplashSecondaryTextBrush}"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Right" />
                </Grid>
                <ProgressBar Grid.Row="2"
                             Style="{StaticResource StartupProgressBarStyle}"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding Progress, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Grid>
</Window>
