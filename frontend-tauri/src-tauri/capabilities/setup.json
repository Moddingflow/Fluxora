{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Fluxora Setup isolated renderer",
  "windows": ["setup"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
